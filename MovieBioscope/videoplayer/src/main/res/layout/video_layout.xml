<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.lib.videoplayer.ui.VideoActivity">

    <include
        android:id="@+id/top_banner_video"
        layout="@layout/top_banner"
        android:visibility="gone" />

    <include
        android:id="@+id/bottom_banner_video"
        layout="@layout/bottom_banner"
        android:visibility="gone" />



    <VideoView
        android:id="@+id/movie_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <VideoView
        android:id="@+id/ad_video"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"/>

</RelativeLayout>
